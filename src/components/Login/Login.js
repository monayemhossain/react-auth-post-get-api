import React from 'react';
import Nav from '../Nav/Nav';

const Login = () => {
    return (
        <div>
            <Nav></Nav>
            <h2> this is login</h2>
        </div>
    );
};

export default Login;